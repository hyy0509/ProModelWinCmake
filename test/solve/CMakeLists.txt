# SET(EXECUTABLE_OUTPUT_PATH ${BIN_DIR})
SET(EXECUTABLE_OUTPUT_PATH ${LIB_DIR})
SET(EXE_NAME solve)
SET(SRC_LIST main.cpp)
INCLUDE_DIRECTORIES(${INC_DIR})
ADD_EXECUTABLE(${EXE_NAME} ${SRC_LIST})
TARGET_LINK_LIBRARIES(${EXE_NAME} ${TARGET_NAME})